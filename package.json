{
  "name": "json-api-entitizer",
  "version": "1.0.1",
  "description": "Package that helps work with json api spec json",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Speakeasy-Team/json-api-entitizer"
  },
  "scripts": {
    "test": "babel-tape-runner tests/**/*.js | tap-spec",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "author": "Elijah Kim",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-tape-runner": "^2.0.1",
    "tap-spec": "^4.1.1"
  },
  "dependencies": {
    "lodash": "^4.5.1",
    "tape": "^4.4.0"
  }
}
